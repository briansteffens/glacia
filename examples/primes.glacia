function main()
{
    found = list(2);
    check = 1;

    next: while (true)
    {
        check = check + 2;

        foreach (previous in found)
        {
            if (previous > check / 2)
                break;

            if (check % previous == 0)
                continue next;
        }

        found.push(check);
        print(check);
    }
}
