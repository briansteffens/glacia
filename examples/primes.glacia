int main()
{
    list found;
    int x = 2;

    while (1)
    {
        int success = 1;
        int i = 0;

        foreach (f in found)
        {
            if (x % f == 0)
            {
                success = 0;
                break;
            }
        }

        if (success == 1)
        {
            found.push(x);
            print(x);
        }

        x = x + 1;
    }
}
