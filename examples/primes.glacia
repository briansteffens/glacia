int main()
{
    list found;
    int x = 2;

    while (true)
    {
        bool success = true;

        foreach (f in found)
        {
            if (x % f == 0)
            {
                success = false;
                break;
            }
        }

        if (success)
        {
            found.push(x);
            print(x);
        }

        x = x + 1;
    }
}
